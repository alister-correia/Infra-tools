# Anthropic — key is supplied per-user at login via the UI, not stored here
# ANTHROPIC_API_KEY=

# VMware VCD environments — comma-separated Name:https://host pairs
# Add as many environments as needed
VCD_ENVIRONMENTS=Prod:https://vcloud.yourdomain.com,Dev:https://vcloud-dev.yourdomain.com
